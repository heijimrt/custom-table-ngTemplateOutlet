import { CustomTableContentDirective } from './custom-table-content.directive';

describe('CustomTableContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTableContentDirective();
    expect(directive).toBeTruthy();
  });
});
